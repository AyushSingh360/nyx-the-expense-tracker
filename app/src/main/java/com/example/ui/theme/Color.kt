package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Sophisticated Dark Premium Palette
val SophisticatedBackground = Color(0xFF0F0F0F)
val SophisticatedSurface = Color(0xFF1E1E1E)
val SophisticatedSurfaceElevated = Color(0xFF2D2D2D)

val OffWhitePrimary = Color(0xFFE5E1DA) // Warm stone/cream white
val GoldAccent = Color(0xFFD4AF37) // Warm bronze gold
val EmeraldAccent = Color(0xFF10B981) // Precision green for positive balances / safe indicators
val RedAccent = Color(0xFFEF4444) // Clean red for expenses

val TextStonePrimary = Color(0xFFE5E1DA)
val TextStoneSecondary = Color(0xFF8F8C85)
val TextStoneMuted = Color(0xFF5A5853)

// Backward compat mappings
val DarkBackground = SophisticatedBackground
val DarkSurface = SophisticatedSurface
val DarkSurfaceElevated = SophisticatedSurfaceElevated
val MintPrimary = OffWhitePrimary
val CyanSecondary = GoldAccent
val NeonPurple = Color(0xFF8B5CF6)
val TextDarkPrimary = TextStonePrimary
val TextDarkSecondary = TextStoneSecondary
val TextDarkMuted = TextStoneMuted
val ExpenseRed = RedAccent
val IncomeGreen = EmeraldAccent
